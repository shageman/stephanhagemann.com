# typed: strict
class Tags::Tag
  sig {params(x: String).void}
  def name=(x)
  end

  sig {params(x: String).void}
  def path=(x)
  end

  sig {params(x: String).void}
  def title=(x)
  end

  sig {params(x: String).void}
  def date=(x)
  end
end
