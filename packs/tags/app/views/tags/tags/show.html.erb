<%
  @layout_params = RailsShims::LayoutParams.new(
    active_section: 'tags',
    container_class: "content-margin-top",
    header: "Content tagged #{tag.name}",
    title: "Content tagged #{tag.name}",
    page_class: "dark-theme theme-blue",
  )
%>

<h1>Content tagged "<%= @tags.first.name %>"</h1>
<div class="blog-list-item">
  <ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.title, tag.path.() %> (<%= Date.parse(tag.date) %>)
    </li>
  <% end %>
  </ul>
</div>

<a href="/tags">All Tags</a>